export interface Video {  
    id: string;
    title: string;
    description: string;
    publicId: string;
    originalSize: string;
    compressedSize: number;
    duration:number;
    createdAt: string;
    updatedAt: string;
}